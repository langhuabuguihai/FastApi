# --- API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1

# --- DB ---
SQLAlchemy==2.0.32
pymysql==1.1.1
# If you STILL use mysql.connector anywhere, keep this.
# Otherwise delete the `import mysql.connector` line and remove this package.
mysql-connector-python==9.0.0

# --- Data/ML stack (versions play nicely together on 3.11) ---
pandas==2.1.4
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.3.2
imbalanced-learn==0.11.0
joblib==1.3.2

# --- Technical indicators / metrics ---
ta==0.11.0
shap==0.43.0   # if this gives build trouble, temporarily remove it to ship first

# --- Market data / utilities ---
yfinance==0.2.43
pandas-datareader==0.10.0
requests==2.32.3

# --- Firebase (only if you actually use it) ---
firebase-admin==6.5.0

# ML libraries (add these lines if missing)
xgboost==2.0.3
